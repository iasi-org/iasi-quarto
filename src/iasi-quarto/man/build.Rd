% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build}
\alias{build}
\title{Build IASI Quarto publications or R packages}
\usage{
build(
  book = NULL,
  format = NULL,
  path = ".",
  outputs = NULL,
  releases = NULL,
  iasi = FALSE,
  force = FALSE
)
}
\arguments{
\item{book}{Publication or publications to build when \code{path} identifies an
IASI Quarto workspace. A publication can be selected by its complete
directory name, its name without the numeric prefix, or its numeric
prefix. Use \code{"all"} or \code{NULL} to build every publication.}

\item{format}{Output format or formats. For R packages use \code{"r"} to build
both source and binary packages, \code{"r-source"} for source only, or
\code{"r-binary"} for binary only. Use \code{"all"} or \code{NULL} to detect the build
type from \code{path}. Other values are delegated to the IASI Quarto builder.}

\item{path}{Directory to build.}

\item{outputs}{Optional output directory name or path. Relative values are
searched from \code{path} upwards. When \code{NULL}, build results are not moved.}

\item{releases}{Optional release directory name or path. Relative values are
searched from \code{path} upwards and attached to the resolved project.}

\item{iasi}{When \code{TRUE}, applies the IASI default for \code{outputs} when no
explicit value is supplied. Defaults to \code{FALSE}.}

\item{force}{Explicitly requests a complete build. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the result produced by the selected builder after
its artifacts have been placed in their output location.
}
\description{
\code{build()} is the public build entry point. It identifies what can be built
from \code{path}, delegates the work to the appropriate private builder, and
places the resulting artifacts in their configured output location.
}
\details{
When \code{format} is \code{NULL} or \code{"all"}, the build type is detected from
\code{path}. An R package is recognised by its \code{DESCRIPTION} file; otherwise the
path is handled by the existing IASI Quarto build pipeline.
}
