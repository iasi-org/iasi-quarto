% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish}
\alias{publish}
\title{Prepare IASI Quarto outputs for deployment}
\usage{
publish(
  book = NULL,
  source = NULL,
  dest = "_publish",
  path = ".",
  force = FALSE
)
}
\arguments{
\item{book}{Publication or publications to publish. A publication can be
selected by its complete directory name, its name without the numeric
prefix, or its numeric prefix. Use \code{"all"} or \code{NULL} to publish every
publication. This argument is ignored when \code{path} is itself a publication.}

\item{source}{Optional output directory to copy. Relative paths are resolved
against each publication root and absolute paths are accepted. When \code{NULL},
infers the common root of the declared Quarto profile outputs.}

\item{dest}{Publication directory relative to each publication root. It must
start with \verb{_} so Quarto ignores the generated tree as project input.
Defaults to \verb{_publish}.}

\item{path}{IASI Quarto publication or multiproject directory.}

\item{force}{Explicitly requests a complete publication pass. \code{publish()}
already republishes the selected output unconditionally; this argument is
provided so callers can propagate a common force policy. Defaults to
\code{FALSE}.}
}
\value{
Invisibly returns the completed \code{iasi_quarto_plan}. Returns \code{NULL}
when \code{path} does not appear to be an IASI Quarto workspace.
}
\description{
Prepares a rendered output tree as a deployable publication package. HTML
contents are placed directly in the publication root, while every other
rendered format keeps its own subdirectory. The source can be selected
explicitly, while the destination is always resolved relative to each
publication root.
}
\details{
After copying, \code{publish()} scans every \code{index.html} inside the published tree.
When an IASI export anchor is present, it is replaced by an Export dropdown
containing links to the export artifacts that actually exist.

Supported anchors are \verb{<!-- IASI_EXPORT -->}, \verb{<div class="iasi-export"></div>}
and \verb{<div class="iasi-export-anchor"></div>}.
}
