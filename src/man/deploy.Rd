% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy.R
\name{deploy}
\alias{deploy}
\title{Build and publish IASI Quarto publications only when required}
\usage{
deploy(
  book = NULL,
  format = NULL,
  dest = "_publish",
  path = ".",
  force = FALSE
)
}
\arguments{
\item{book}{Publication or publications to deploy. Uses the same selection
rules as \code{\link[=build]{build()}} and \code{\link[=publish]{publish()}}.}

\item{format}{Output format or formats to keep current. Uses the same
selection rules as \code{\link[=build]{build()}}.}

\item{dest}{Publication directory relative to each selected Quarto project.
The first path component must start with \verb{_}. Defaults to \verb{_publish}.}

\item{path}{IASI Quarto publication or multiproject directory.}

\item{force}{When \code{TRUE}, freshness is still evaluated but both build and
publish are executed for every selected publication. Validation and
execution errors are still enforced. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the plan produced by the last operation executed.
When no build or publish is required, returns the checked deployment plan.
Returns \code{NULL} when \code{path} does not appear to be an IASI Quarto workspace.
}
\description{
Deploys IASI Quarto publications incrementally, one publication at a time.
Each publication keeps its operational state in \code{.iasi/quarto/state.yml}.
Source and build output fingerprints are used independently for every
selected publication to decide whether it needs a build, a publish, both,
or no work at all.
}
\details{
For each publication, \code{deploy()} first checks whether a build is required.
After any required build completes, it checks the resulting output again and
publishes only that publication when its output differs from the last
published output. This keeps build and publish decisions local to each
publication instead of batching all builds before all publishes.
}
\examples{
\dontrun{
deploy()
}

}
\seealso{
\code{\link[=build]{build()}}, \code{\link[=publish]{publish()}}
}
