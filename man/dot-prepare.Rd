% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine-prepare.R
\name{.prepare}
\alias{.prepare}
\title{Prepare IASI Quarto publications}
\usage{
.prepare(plan)
}
\arguments{
\item{plan}{An \code{iasi_quarto_plan} returned by \code{\link[=check]{check()}}.}
}
\value{
Invisibly returns the same \code{iasi_quarto_plan}, enriched with one
\code{iasi_quarto_publication} in each project and aggregate \code{prepared} and
\code{changed} flags.
}
\description{
Generates the derived files required by every checked publication.
}
\details{
\code{prepare()} only accepts a valid plan returned by \code{\link[=check]{check()}}. It does not
discover, check or render publications.

For book publications, preparation depends on the IASI publication strategy.
\code{regular}, \code{structured}, and \code{direct} books generate the derived book
structure required by Quarto.

Website publications currently require no derived artifacts and preparation
only creates the runtime publication descriptor used by the render stage.
}
