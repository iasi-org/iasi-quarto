# Outputs and releases

Source: working conversations with Javier G. Grandez.

`outputs/` and `releases/` do not live inside the IASI project source repository.

The project contains the knowledge and sources required to produce artifacts. `outputs/` contains generated materialisations. `releases/` contains prepared or accepted distributable materialisations.
