# Output routing

Source: working conversations with Javier G. Grandez.

Final IASI outputs live outside the project source tree.

Software package results are placed below the global `outputs/software/` area.

Documentation results are placed below the global `outputs/docs/` area.

The exact final path is resolved by the common move phase after the builder has finished.
