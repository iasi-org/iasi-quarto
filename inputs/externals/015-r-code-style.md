# R code style used in IASI Quarto

Source: working conversations with Javier G. Grandez.

In the R code of `iasi-quarto`:

- Use `=` rather than `<-` for assignment.
- Keep function-call parameters on one line while the call remains readable; do not put every argument on a separate line by default.
