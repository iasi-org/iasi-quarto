# Output naming

Source: working conversations with Javier G. Grandez.

When a source artifact directory begins with an ordering number, that ordering prefix is not part of the published output name.

Examples:

- `01-user-guide` becomes `user-guide`.
- `10-part-I` becomes `part-I`.
- `3_docs` becomes `docs`.
