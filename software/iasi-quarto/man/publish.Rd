% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish}
\alias{publish}
\title{Prepare IASI Quarto outputs for deployment}
\usage{
publish(book = NULL, source = NULL, path = ".", force = FALSE)
}
\arguments{
\item{book}{Publication or publications to publish. A publication can be
selected by its complete directory name, its name without the numeric
prefix, or its numeric prefix. Use \code{"all"} or \code{NULL} to publish every
publication. This argument is ignored when \code{path} is itself a publication.}

\item{source}{Optional build-output root to publish. Relative paths are
resolved against each publication root and absolute paths are accepted.
When \code{NULL}, the common root of the configured output profiles is used.}

\item{path}{IASI Quarto publication or multiproject directory.}

\item{force}{Reserved for API symmetry with \code{\link[=build]{build()}} and \code{\link[=deploy]{deploy()}}.
\code{publish()} already performs the requested publication pass
unconditionally.}
}
\value{
Invisibly returns the completed \code{iasi_quarto_plan}. Returns \code{NULL}
when \code{path} does not appear to be an IASI Quarto workspace.
}
\description{
Materialises previously built outputs into one workspace-level publication
tree without rendering sources again.
}
\details{
For a single publication, the default destination is \verb{<publication>/_publish}.
For a multiproject workspace, all selected publications share one
\verb{<workspace>/_publish} root and each publication is written below a stable
child directory derived from its folder name with any leading numeric prefix
removed.

Publishing one child publication replaces only that child directory. Sibling
publications already present below \verb{_publish} are preserved.

HTML contents are moved to the publication root while other formats keep
their own subdirectories. Export links and publication metadata are then
normalised against the resulting tree.
}
