% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy.R
\name{deploy}
\alias{deploy}
\title{Build and publish IASI Quarto publications only when required}
\usage{
deploy(book = NULL, format = NULL, path = ".", force = FALSE)
}
\arguments{
\item{book}{Publication or publications to deploy. Uses the same selection
rules as \code{\link[=build]{build()}} and \code{\link[=publish]{publish()}}.}

\item{format}{Output format or formats to keep current. Uses the same
selection rules as \code{\link[=build]{build()}}.}

\item{path}{IASI Quarto publication or multiproject directory.}

\item{force}{When \code{TRUE}, both build and publish are executed for every
selected publication even when fingerprints are current. Validation and
execution errors are still enforced. Defaults to \code{FALSE}.}
}
\value{
Invisibly returns the final deployment \code{iasi_quarto_plan}, including
the updated project objects and deployment counters. Returns \code{NULL} when
\code{path} does not appear to be an IASI Quarto workspace.
}
\description{
Keeps selected publications current using local fingerprints. Each
publication is evaluated independently: \code{deploy()} builds it only when its
source or requested build output is stale, then publishes it only when the
resulting build tree differs from the last successful publication.
}
\details{
In a multiproject workspace, publication destinations are resolved once from
the workspace root. This preserves the single \verb{_publish} tree even though an
individual build is executed from the child publication directory.
}
\examples{
\dontrun{
deploy()
}

}
\seealso{
\code{\link[=build]{build()}}, \code{\link[=publish]{publish()}}
}
