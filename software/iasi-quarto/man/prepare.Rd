% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare.R
\name{prepare}
\alias{prepare}
\title{Prepare IASI Quarto publications}
\usage{
prepare(book = NULL, path = ".")
}
\arguments{
\item{book}{Publication or publications to prepare. A publication can be
selected by its complete directory name, its name without the numeric
prefix, or its numeric prefix. Use \code{"all"} or \code{NULL} to prepare every
publication. This argument is ignored when \code{path} is itself a publication.}

\item{path}{IASI Quarto publication or multiproject directory.}
}
\value{
Invisibly returns the prepared \code{iasi_quarto_plan}. Returns \code{NULL}
when \code{path} does not appear to be an IASI Quarto workspace.
}
\description{
Validates an IASI Quarto workspace and materializes the derived artifacts
required by one or more selected publications without rendering them.
}
\details{
\code{prepare()} runs the same recognition, discovery, checking, and preparation
stages used by \code{\link[=build]{build()}}, but stops before invoking Quarto. It is intended
for workflows where generated artifacts such as \verb{_book-structure.yml} or
\verb{_generated/navbar-left.yml} need to be reviewed before rendering.
}
